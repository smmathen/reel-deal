/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
module.exports = {
  images: {
    domains: ['media.posterlounge.com'],
  },
};

module.exports = nextConfig

